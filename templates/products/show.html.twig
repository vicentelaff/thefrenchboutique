{% extends 'base.html.twig' %}

{% block title %}
	{{ product.name }}
	- The French Boutique
{% endblock %}

{% block content %}

	<div class="row">
		<div class="col-md-5">
			<img src="/uploads/{{ product.image }}" alt="{{ product.name }}" class="img-fluid">
		</div>

		<div class="col-md-7 my-auto">
			<h3>{{ product.name }}</h3>
			<p>{{ product.subtitle }}</p>
			<span class="single-product-price text-success">{{ product.price / 100 }}€</span>
			<hr>
			<p>{{ product.description }}</p>
			<a href="" class="btn btn-success">Add to basket</a>
		</div>
	</div>


{% endblock %}
